<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container px-4 px-lg-5">
    <a class="navbar-brand" href="#"><%= t('.title') %></a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span></button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0 ms-lg-4">
        <% if session[:user_id] %>
          <li class="nav-item"><a class="nav-link"><%= link_to 'Orders', orders_path %></li>
          <li class="nav-item"><a class="nav-link"><%= link_to 'Products', products_path %></li>
          <li class="nav-item"><a class="nav-link"><%= link_to 'Users', users_path %></li>
          <li class="nav-item"><a class="nav-link"><%= button_to 'Logout', logout_path, method: :delete %></li>
        <% else %>
          <li class="nav-item"><a class="nav-link active" aria-current="page" href="/"><%= t('.home') %></a></li>
          <li class="nav-item"><a class="nav-link" href="#"><%= t('.questions') %></a></li>
          <li class="nav-item"><a class="nav-link" href="#"><%= t('.news') %></a></li>
          <li class="nav-item"><a class="nav-link" href="#"><%= t('.contact') %></a></li>
        <% end %>
      </ul>
      <form class="d-flex">
        <button class="btn btn-outline-dark" type="submit">
          <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi-cart-fill me-1" viewBox="0 0 16 16">
            <path d="M0 1.5A.5.5 0 0 1 .5 1H2a.5.5 0 0 1 .485.379L2.89 3H14.5a.5.5 0 0 1 .491.592l-1.5 8A.5.5 0 0 1 13 12H4a.5.5 0 0 1-.491-.408L2.01 3.607 1.61 2H.5a.5.5 0 0 1-.5-.5zM5 12a2 2 0 1 0 0 4 2 2 0 0 0 0-4zm7 0a2 2 0 1 0 0 4 2 2 0 0 0 0-4zm-7 1a1 1 0 1 1 0 2 1 1 0 0 1 0-2zm7 0a1 1 0 1 1 0 2 1 1 0 0 1 0-2z"/>
          </svg>
          Cart
          <span class="badge bg-dark text-white ms-1 rounded-pill"><%= @cart ? @cart.line_items.size : @cart %></span>
        </button>
      </form>
      
        <%= form_tag store_index_path, class: 'locale' do %>
          <%= select_tag 'set_locale',
                         options_for_select(LANGUAGES, I18n.locale.to_s),
                         {onchange: 'this.form.submit()', class: 'form-select'} %>
        <% end %>

    </div>
  </div>
</nav>

<header class="bg-dark py-2">
  <div class="container px-2 px-lg-5 my-5">
    <div class="text-center text-white">
      <h1 class="display-8 fw-bolder">Shop in style</h1>
      <p class="lead fw-normal text-white-50 mb-0">With this shop site</p>
    </div>
  </div>
</header>